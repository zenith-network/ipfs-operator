apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: ipfs-operator-web
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: ipfs-operator-web
  basicAuth:
    users:
      name: "ipfs-operator-web-basic-auth"
